
name: Deploy in kubernetes

on:
  push:
    branches: [ Hometask_8 ]
    
jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:

    # - name: Checkout source code
    #   run: cd .. && ls -la && pwd
    # env:
    #   workdir-directory: ./SergeyLadutko/Hometask_8/
    # steps:
    # - uses: actions/checkout@master
    # - name: Run Snyk to check for vulnerabilities
    #   uses: snyk/actions/node@master
    #   env:
    #     SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    #   with:
    #     command: test ./SergeyLadutko/Hometask_8/
    - uses: actions/checkout@v1
    - name: Login to DockerHub Registry
      run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
    - name: Build the tagged Docker image
      run: docker build -t sergeyladutko/kuber:latest ./SergeyLadutko/Hometask_8/ 
    - name: Push the tagged Docker image
      run: docker push sergeyladutko/kuber:latest
    # - name: Build the latest Docker image
    #   run: docker build ./SergeyLadutko/Hometask_8/ --file Dockerfile --tag sergeyladutko/kuber:latest
    # - name: Push the latest Docker image
    #   run: docker push sergeyladutko/kuber:latest
    # - name: Publish to Registry
    #   uses: docker/build-push-action@v1
    #   # run: ls -la && pwd
    #   with:
    #     repository: sergeyladutko/kuber
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    #     tags: latest
    #     DOCKERFILE: SergeyLadutko/Hometask_8/Dockerfile
    #     path: SergeyLadutko/Hometask_8/
    #     dockerfile: Dockerfile

